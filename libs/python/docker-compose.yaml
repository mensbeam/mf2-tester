name: mftester
services:
  python:
    build: .
    volumes:
      - .:/app/libs/python
      - ./.docker-home:/home
      - ../../do:/app/do:ro
      - ../../resources:/app/resources:ro
      - ../../results:/app/results