name: mftester
services:
  php:
    build: .
    volumes:
      - .:/app/libs/php
      - ./.docker-home:/home
      - ../../do:/app/do:ro
      - ../../resources:/app/resources:ro
      - ../../results:/app/results