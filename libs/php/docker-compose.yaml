name: mftester
services:
  php:
    build: .
    volumes:
      - .:/app/libs/php
      - ./.docker-home:/home
      - ../../do:/app/do:ro
      - ../../normalize.jq:/app/normalize.jq:ro
      - ../../results:/app/results