name: mftester
services:
  js:
    build: .
    volumes:
      - .:/app/libs/js
      - ./.docker-home:/home
      - ../../do:/app/do:ro
      - ../../resources:/app/resources:ro
      - ../../results:/app/results