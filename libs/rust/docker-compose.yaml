name: mftester
services:
  rust:
    build: .
    volumes:
      - .:/app/libs/rust
      - ./.docker-home:/home
      - ../../do:/app/do:ro
      - ../../resources:/app/resources:ro
      - ../../results:/app/results