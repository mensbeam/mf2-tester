name: mftester
services:
  go:
    build: .
    volumes:
      - .:/app/libs/go
      - ./.docker-home:/home
      - ../../do:/app/do:ro
      - ../../resources:/app/resources:ro
      - ../../results:/app/results