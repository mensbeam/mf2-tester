name: mftester
services:
  perl:
    build: .
    volumes:
      - .:/app/libs/perl
      - ./.docker-home:/home
      - ../../do:/app/do:ro
      - ../../normalize.jq:/app/normalize.jq:ro
      - ../../results:/app/results