name: mftester
services:
  perl:
    build: .
    volumes:
      - .:/app/libs/perl
      - ./.docker-home:/home
      - ../../do:/app/do:ro
      - ../../resources:/app/resources:ro
      - ../../results:/app/results