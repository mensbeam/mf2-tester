name: mftester
services:
  ruby-micromicro:
    build: .
    volumes:
      - .:/app/libs/ruby-micromicro
      - ./.docker-home:/home
      - ../../do:/app/do:ro
      - ../../resources:/app/resources:ro
      - ../../results:/app/results