name: mftester
services:
  ruby:
    build: .
    volumes:
      - .:/app/libs/ruby
      - ./.docker-home:/home
      - ../../do:/app/do:ro
      - ../../resources:/app/resources:ro
      - ../../results:/app/results