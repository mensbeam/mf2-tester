#!/usr/bin/env bash

if [ "$1" = "test" ]; then
    shift; ruby "test-one.rb" $@
elif [ "$1" = "version" ]; then
    bundle info microformats --version
elif [ "$1" = "compile" ]; then
    true
elif [ "$1" = "setup" ]; then
    bundle install --quiet
elif [ "$1" = "update" ]; then
    bundle update --all --quiet
fi