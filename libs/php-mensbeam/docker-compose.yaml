name: mftester
services:
  php-mensbeam:
    build: .
    volumes:
      - .:/app/libs/php-mensbeam
      - ./.docker-home:/home
      - ../../do:/app/do:ro
      - ../../normalize.jq:/app/normalize.jq:ro
      - ../../results:/app/results