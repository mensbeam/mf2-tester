name: mftester
services:
  php-mensbeam:
    build: .
    volumes:
      - .:/app/libs/php-mensbeam
      - ./.docker-home:/home
      - ../../do:/app/do:ro
      - ../../resources:/app/resources:ro
      - ../../results:/app/results