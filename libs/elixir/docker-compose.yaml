name: mftester
services:
  elixir:
    build: .
    volumes:
      - .:/app/libs/elixir
      - ./.docker-home:/home
      - ../../do:/app/do:ro
      - ../../resources:/app/resources:ro
      - ../../results:/app/results