name: mftester
services:
  csharp:
    build: .
    volumes:
      - .:/app/libs/csharp
      - ./.docker-home:/home
      - ../../do:/app/do:ro
      - ../../resources:/app/resources:ro
      - ../../results:/app/results